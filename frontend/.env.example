# Public URL of Django backend for browser requests
# Use localhost because requests are sent from the browser, not from Docker network.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/
# Backward-compatible alias supported by frontend constants
# NEXT_PUBLIC_API_URL=http://localhost:8000/
